import { ComponentFixture, TestBed } from '@angular/core/testing';
import { GestorrecompensaspagePage } from './gestorrecompensaspage.page';

describe('GestorrecompensaspagePage', () => {
  let component: GestorrecompensaspagePage;
  let fixture: ComponentFixture<GestorrecompensaspagePage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(GestorrecompensaspagePage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
