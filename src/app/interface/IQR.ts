export interface QRCodeData {
  qrData: string; // Asegúrate de que esto refleje la estructura real de tus datos
  userId: string;
  eventosInscritos: string[];
  // Otras propiedades que esperes en tu documento
}
